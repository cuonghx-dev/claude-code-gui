// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { MemoryKind } from "./MemoryKind";
import type { MemoryScope } from "./MemoryScope";

export type MemoryFile = { 
/**
 * `<scope>:<kind>:<relative path>`.
 */
id: string, kind: MemoryKind, scope: MemoryScope, path: string, relPath: string, 
/**
 * False for a file Claude Code would read if it existed — the UI offers
 * to create it.
 */
exists: boolean, sizeBytes: number | null, mtimeMs: number | null, 
/**
 * First markdown heading, for the list.
 */
title: string | null, importCount: number, };
