// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { ClaudeDirKind } from "./ClaudeDirKind";

/**
 * One node of the explorer tree. Documented entries are always returned even
 * when missing from disk (`exists: false`) so the UI can show what *could*
 * live there; anything else found on disk comes back with `known: false`.
 */
export type ClaudeDirEntry = { 
/**
 * Stable id for documented entries (`global-settings`, …); for on-disk
 * extras it is the absolute path.
 */
id: string, label: string, path: string, kind: ClaudeDirKind, exists: boolean, 
/**
 * `true` when this entry comes from the documented catalog.
 */
known: boolean, sizeBytes: bigint | null, 
/**
 * Immediate child count for directories.
 */
childCount: number | null, modifiedAt: string | null, 
/**
 * One-line description from the docs. Empty for on-disk extras.
 */
oneLiner: string, 
/**
 * `committed`, `gitignored` or `local` — how the file is usually tracked.
 */
badge: string | null, docsUrl: string | null, 
/**
 * Whether this directory has anything in it, so the UI knows to offer an
 * expander without listing the contents first.
 */
hasChildren: boolean, 
/**
 * Set on the last child when a listing hit the cap.
 */
truncated: boolean, 
/**
 * Symlinks are shown but never descended.
 */
isSymlink: boolean, 
/**
 * Always empty: children are fetched on demand through
 * `claude_directory_children`.
 */
children: Array<ClaudeDirEntry>, };
