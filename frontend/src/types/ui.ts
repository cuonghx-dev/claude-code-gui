// UI-only types. Server payload types live in `frontend/src/types/ipc/`
// (generated by ts-rs).

export type Theme = 'light' | 'dark' | 'system'

export interface Toast {
  id: string
  kind: 'success' | 'error' | 'info' | 'warning'
  message: string
}
